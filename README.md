## Getting Started

To run this project, ensure the link that the csv is getting pulled from is correct 
Other than that just run main

## Folder Structure

- `src`: the folder to maintain sources
- `lib`: the folder to maintain dependencies

